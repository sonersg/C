#include <stdio.h>
#include <math.h>

int main(){

    double A = sqrt(9);
    double B = pow(2, 6);
    int C = round(3.14);
    int D = ceil(3.009);
    int E = floor(3.99);
    double F = fabs(-21);
    double G = log(3);
    double H = sin(45);
    double I = cos(45);
    double J = tan(45);

    printf("%d", D);
    // printf("%f", H);

    return 0;
}
